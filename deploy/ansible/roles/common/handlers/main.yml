---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart scangrid
  ansible.builtin.systemd:
    name: "{{ scangrid_unit }}"
    state: restarted
    enabled: true
